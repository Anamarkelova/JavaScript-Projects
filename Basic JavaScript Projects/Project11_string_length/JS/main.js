// Create a string.
var text = "Hello World";

// Use the length property to count the characters in the string.
var textLength = text.length;

// Display the number of characters on the webpage.
document.getElementById("Length").innerHTML =
    "The string \"" + text + "\" contains " + textLength + " characters.";